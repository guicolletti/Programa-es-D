const aluno = "Francisco Junior"
const nota1 = 8.9
const nota2 = 3.4
const media = (nota1 + nota2) / 2

if (media >= 6) {
    console.log(`O aluno ${aluno} está aprovado com média ${media}`)
    console.log(media)
} else {
    console.log(`O aluno ${aluno} está reprovado com média ${media}`)
    console.log(media)
}