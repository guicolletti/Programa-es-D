const bairros = [
    "Centro", "Vila Fátima", "Jaraguá", "Campestre", 
    "Artemis", "Cecap", "Vale do Sol", "Vila Industrial", 
    "Mário Dedini", "Ondas", "Paulista" 
]

for (var i = 0; i < bairros.length; i++) {
    const bairro = bairros[i];
    console.log(`O bairro é ${bairro}`)
}